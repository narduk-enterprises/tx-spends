# Copy to `doppler.yaml` in this directory (gitignored). Then `pnpm run dev`
# and other `doppler run -- …` scripts use the right project/config.
#
# Replace `project` with your Doppler app name before local Doppler commands.
setup:
  project: tx-spends
  config: dev
