# **DISPLAY_NAME**

Starter generated from `narduk-nuxt-template`.

## Workspace Shape

- `apps/web/` is the shipped application.
- `deploy/preview/` holds the repo-managed preview assets.
- `packages/eslint-config/` contains the shared lint plugins.
- `tools/` and `scripts/` contain local automation and helper commands.
- The app depends on a published bundle package by default and does not vendor
  `layers/` in starter exports.
